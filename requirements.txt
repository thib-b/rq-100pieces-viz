# Blender Headless Visualization Pipeline Dependencies
# 
# Note: Blender ships with its own Python interpreter, so these packages
# would need to be installed in Blender's embedded Python environment.
# For basic functionality, no additional packages are required.

# Optional: blenderless package for easier management of multiple renders
# pip install blenderless

# For development/testing outside of Blender (not for use with bpy)
# numpy>=1.0.0
# pillow>=8.0.0
