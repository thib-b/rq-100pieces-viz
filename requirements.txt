# Runtime: the render engine runs inside Blender's bundled Python, which already ships
# numpy — so there is nothing to pip-install to render. External tools required on PATH:
#   * Blender 5.x            (https://www.blender.org/)  -> /opt/homebrew/bin/blender
#   * ffmpeg                 (for the auto-encode step)
#
# To run the test harness (outside Blender), install the dev deps into a venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt
